// JavaScript Document
var getQuotes = function() {
	var quotes = ["Do unto others before they do unto you", "Time is money", "Tempis fugit"];
	var quotesIndex = Math.floor(Math.random() * quotes.length + 1);
	var quote = quote[quoteIndex];
	return quote;
}
	